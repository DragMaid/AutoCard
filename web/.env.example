# Base URL of the Java game API (Socket.IO endpoint).
VITE_GAME_API=http://localhost:8080

# Where the shared assets/ directory is served from. Defaults to /assets, which
# the dev server maps to the repository's assets/ folder via web/public/assets.
VITE_ASSET_BASE=/assets
